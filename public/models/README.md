This site loads two local GLB models:

- Hero model: `drone-hero.glb` → `/models/drone-hero.glb`
- Showcase model: `drone-showcase.glb` → `/models/drone-showcase.glb`

If either file is missing or fails to load, the site automatically uses a procedural low‑poly hexacopter fallback.

Model credit requirement:
“Drone For Agriculture (Low Poly)” by Bharat Nagar — Creative Commons Attribution (Sketchfab)
